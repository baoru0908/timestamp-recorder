# Gradle / 构建产物
.gradle/
build/
build_log.txt
release_log.txt
captures/
.externalNativeBuild/
.cxx/

# 本机配置（含 Android SDK 路径）
local.properties

# 字体源文件：官方全量的 MiSans（约 7.5MB/字重），只用于
# 跑 tools/subset_misans.py 重新生成 assets/fonts 下的子集，不入库
.fontsrc/

# IDE
.idea/
*.iml
.DS_Store

# Python 工具缓存
__pycache__/
*.pyc

# 本机调试产物：adb 截图、裁图、图标预览等临时文件，不入库
# ⚠️ 末行原为损坏规则 `*.png
w*.txt`（`
` 是字面反斜杠+n，解析成单个不生效的 pattern），
#    v4.5.0 按原意拆成下面几条：临时 PNG 与 `_w*.txt` 都不入库，仅放行 docs/images 正式截图。
_*.png
*.png
!docs/images/*.png
_w*.txt

# 交付安装包与签名密钥：严禁入库
# （安装包请走 GitHub Releases，不要放进仓库）
*.apk
*.aab
*.idsig
*.keystore
*.jks

# 交接文档：含签名密码等敏感信息，仅本机留存、不上传，
# 但开发过程中会持续更新，供后续接手者阅读
DEVELOPMENT.md
